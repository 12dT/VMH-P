# Local Artifacts

This directory is reserved for optional local experiment artifacts.

`checkpoints/main_table_seed42/` documents the local layout used for the nine
host/dataset settings in the current main table. Checkpoint files, cached host
representations, and other binary experiment artifacts are kept local because
several files exceed GitHub's normal file-size limits and may be governed by the
licenses of the original backbone or dataset providers.
