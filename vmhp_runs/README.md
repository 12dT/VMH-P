# VMH-P Result Summaries

This directory is reserved for lightweight VMH-P result summaries and selected
paper-ready figures.

The anonymous release intentionally excludes large checkpoints, cached feature
tensors, raw logs, and legacy intermediate experiment folders. The main
numbers, default hyperparameters, and reproduction commands are documented in
the top-level `README.md`.
